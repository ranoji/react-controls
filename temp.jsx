 <Typography
      component="a" // Render Typography as an <a> element
      href="#" // Add the hyperlink target
      onClick={handleClick} // Handle click event
      sx={{ textDecoration: "underline", color: "blue", cursor: "pointer" }}
    >
      Click here to view details
    </Typography>
